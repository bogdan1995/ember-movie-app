import { fixedNumber } from 'movie-app/helpers/fixed-number';
import { module, test } from 'qunit';

module('Unit | Helper | fixed number');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fixedNumber([42]);
  assert.ok(result);
});
