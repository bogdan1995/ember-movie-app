import { movieImage } from 'movie-app/helpers/movie-image';
import { module, test } from 'qunit';

module('Unit | Helper | movie image');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = movieImage([42]);
  assert.ok(result);
});
